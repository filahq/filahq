<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&display=swap"
			rel="stylesheet"
		/>
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="Statify by FilaHQ helps Filament developers expose stats widgets securely and view them across Chrome and future surfaces."
		/>
		<meta property="og:type" content="website" />
		<meta property="og:title" content="FilaHQ | Statify for Filament" />
		<meta
			property="og:description"
			content="Statify by FilaHQ helps Filament developers expose stats widgets securely and view them across Chrome and future surfaces."
		/>
		<meta property="og:image" content="/og-statify.png" />
		<meta property="og:image:width" content="1200" />
		<meta property="og:image:height" content="630" />
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content="FilaHQ | Statify for Filament" />
		<meta
			name="twitter:description"
			content="Statify by FilaHQ helps Filament developers expose stats widgets securely and view them across Chrome and future surfaces."
		/>
		<meta name="twitter:image" content="/og-statify.png" />
		<title>FilaHQ | Statify for Filament</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	:root {
		color-scheme: light;
		--color-bg: #f7f8fc;
		--color-bg-soft: #ffffff;
		--color-panel: rgba(255, 255, 255, 0.82);
		--color-line: rgba(15, 23, 42, 0.1);
		--color-text: #0f172a;
		--color-text-soft: rgba(15, 23, 42, 0.62);
		--color-signal: #ffc400;
		--color-purple: #6d39f2;
		--color-shadow: rgba(30, 41, 59, 0.15);
		--font-sans: 'Space Grotesk', 'Avenir Next', 'Segoe UI', sans-serif;
		--font-mono: 'SFMono-Regular', 'SF Mono', 'Menlo', monospace;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100%;
	}

	html {
		background: var(--color-bg);
		scroll-behavior: smooth;
	}

	body {
		background:
			radial-gradient(circle at 78% 8%, rgba(109, 57, 242, 0.12), transparent 24rem),
			radial-gradient(circle at 12% 18%, rgba(255, 196, 0, 0.14), transparent 22rem),
			linear-gradient(180deg, #ffffff 0%, var(--color-bg) 45%, #eef2f8 100%);
		color: var(--color-text);
		font-family: var(--font-sans);
	}

	* {
		box-sizing: border-box;
	}

	a {
		color: inherit;
	}

	a:visited {
		color: inherit;
	}
</style>
